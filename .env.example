# Copy this file to `.env` for local secrets, then run/build with:
#   flutter run --dart-define-from-file=.env
#
# `.env` is gitignored; in CI these values come from repo secrets instead.
#
# There are currently NO required keys — map tiles use OpenFreeMap, which is
# keyless. This file is kept as the place to add any future API keys.
